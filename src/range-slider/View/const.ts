const RANGE_SLIDER = 'range-slider';
const TRACK = `${RANGE_SLIDER}__track`;
const PROGRESS = `${RANGE_SLIDER}__progress`;
const SCALE = `${RANGE_SLIDER}__scale`;
const SCALE_ITEM = `${RANGE_SLIDER}__scale-item`;
const THUMB = `${RANGE_SLIDER}__thumb`;
const TOOLTIP = `${RANGE_SLIDER}__tooltip`;

const PROGRESS_HIDDEN = `${PROGRESS}_hidden`;
const SCALE_HIDDEN = `${SCALE}_hidden`;
const THUMB_HIDDEN = `${THUMB}_hidden`;
const THUMB_DRAGGED = `${THUMB}_dragged`;
const TOOLTIP_HIDDEN = `${TOOLTIP}_hidden`;

export {
  RANGE_SLIDER,
  TRACK,
  PROGRESS,
  SCALE,
  SCALE_ITEM,
  THUMB,
  TOOLTIP,
  PROGRESS_HIDDEN,
  SCALE_HIDDEN,
  THUMB_HIDDEN,
  THUMB_DRAGGED,
  TOOLTIP_HIDDEN,
};
