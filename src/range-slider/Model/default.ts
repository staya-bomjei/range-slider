import { ModelOptions } from './types';

export default {
  min: 0,
  max: 100,
  step: 1,
  valueFrom: 39,
  valueTo: 45,
  // strings: ['uno', 'dablo', 'tri', 'four', 'five'],
  // orientation: 'horizontal',
  orientation: 'vertical',
  scaleParts: 4,
  isRange: true,
  showScale: true,
  showTooltip: true,
  showProgress: true,
} as ModelOptions;
