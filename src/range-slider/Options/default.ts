import { Options } from './types';

export default {
  min: 0,
  max: 100,
  step: 1,
  orientation: 'horizontal',
  scaleParts: 5,
  showScale: true,
  showTooltip: true,
  showProgress: true,
} as Options;
