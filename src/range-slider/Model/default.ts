import { ModelOptions } from './types';

export default {
  min: 0,
  max: 100,
  step: 5,
  valueFrom: 25,
  valueTo: 50,
  // strings: ['uno', 'dablo', 'tri', 'four', 'five'],
  orientation: 'horizontal',
  scaleParts: 4,
  isRange: true,
  showScale: true,
  showTooltip: true,
  showProgress: true,
} as ModelOptions;
